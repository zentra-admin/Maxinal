import { randomUUID } from "node:crypto"; import { application } from "../../../../../_composition"; import { startPreview } from "../../../../../../../domain/maxinal-api"; export async function POST(_request: Request, context: { params: Promise<{ projectId: string; versionId: string }> }) { const p = await context.params; const result = await startPreview(application, { ...p, previewSessionId: randomUUID() }); return Response.json(result, { status: result.ok ? 201 : 404 }); }
